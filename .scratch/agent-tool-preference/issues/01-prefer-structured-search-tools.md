> *This was generated by AI during triage.*

# Prefer structured search tools over bash grep

**Category:** enhancement
**Status:** needs-triage

## Report

pi Agent 的行为逻辑不符合预期：Agent 更喜欢用 `bash` 工具跑 grep，而不是用 `grep` 工具或 pi-fff 扩展（`ffgrep`/`fffind`）。

## Triage Notes

**已确认（验证通过）：**

- 本会话中 agent 多次用 `bash` + `grep -rn` 搜索，而非 `grep` 工具或 `ffgrep`（会话历史 recall 证据）。
- pi-fff（`@ff-labs/pi-fff` v0.10.6）处于默认 `tools-and-ui` 模式：无 `pi-fff.json`、无 `FFF_*` 环境变量，内置 `grep`/`find` 工具与 `ffgrep`/`fffind` 并存 → 三条重叠路径（bash grep、内置 grep 工具、ffgrep）。
- 扩展的 `promptGuidelines` 只教 ffgrep 用法（裸标识符、path 限定、caseSensitive），没有任何"优先用工具而非 bash"的指引；AGENTS.md 也没有。
- 根因：工具冗余 + 无行为指引 + 模型习惯（bash grep 是通用模式）。

**修复方案（待定）：**

- A. pi-fff 切 `override` 模式（建 `~/.pi/agent/pi-fff.json` 设 `"mode": "override"`，内置 grep/find 被 FFF 同名替换，需 `/reload`）+ 全局 AGENTS.md 加行为规则（优先用 grep/ffgrep 工具，不用 bash 跑 grep）
- B. 只切 override 模式
- C. 只加行为规则（全局 AGENTS.md）

## Comments

- 2026-09-08: 用户选择「先记录，不改」。状态保持 `needs-triage`，以后再处理。
