{
  "name": "pi-extensions",
  "private": true,
  "type": "module",
  "description": "Local pi extensions (dev deps + bundling for startup speed)",
  "scripts": {
    "build": "tsdown",
    "build:watch": "tsdown --watch",
    "test": "for f in test/*.test.mjs; do echo \"== $f\"; node \"$f\" || exit 1; done",
    "typecheck": "./tsgo",
    "test:extensions": "cd codegraph && node test/harness.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "tsdown": "^0.23.0",
    "vitest": "3.2.6"
  }
}
